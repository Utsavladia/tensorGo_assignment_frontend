import React, { useState } from "react";
import axios from "axios";

const Commnets = ({ category }) => {
  const [comments, setComments] = useState([]);
  try {
  } catch (error) {}
  return <div className="h-full w-full bg-black mb-1"></div>;
};

export default Commnets;
